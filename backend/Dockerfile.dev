FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install all dependencies (including dev dependencies)
RUN npm install

# Expose port
EXPOSE 3001

# Start with nodemon for hot-reloading
CMD ["npm", "run", "dev"]
